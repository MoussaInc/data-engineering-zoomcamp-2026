name: nyc-taxi-pipeline
description: A data pipeline for processing NYC taxi data.
schedule: "daily"

start_date: "2022-01-01"

default_connections:
  duckdb: duckdb-default

assets:
  - path: assets
  
variables:
  taxi_types:
    type: array
    items:
      type: string 
    default: ["yellow"]